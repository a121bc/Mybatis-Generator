package [[${prefix}]].module.[[${ptablename}]].service.impl;

import [[${prefix}]].module.[[${ptablename}]].mapper.[[${utablename}]]Mapper;
import [[${prefix}]].module.[[${ptablename}]].service.I[[${utablename}]]Service;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
[# th:if="${extend} eq 1"]import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import [[${prefix}]].module.[[${ptablename}]].po.[[${utablename}]];[/]
[# th:if="${extend} ne 1"]import org.springframework.beans.factory.annotation.Autowired;[/]

@Service
@Transactional
public class [[${utablename}]]ServiceImpl [# th:if="${extend} eq 1"]extends ServiceImpl<[[${utablename}]]Mapper, [[${utablename}]]> [/]implements I[[${utablename}]]Service {
[# th:if="${extend} ne 1"]
    @Autowired
    private [[${utablename}]]Mapper [[${beanname}]]Mapper;
[/]
}
